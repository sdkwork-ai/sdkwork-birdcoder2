# config/host/

Flutter platform packaging metadata for `sdkwork-birdcoder2-flutter-mobile`: bundle/package ids, permission references,
push environment, app links and universal links, and signing **reference** names
(`FLUTTER_APP_MOBILE_ARCHITECTURE_SPEC.md` §9). Natives generated by Flutter tooling are platform
outputs and must not own business logic.
