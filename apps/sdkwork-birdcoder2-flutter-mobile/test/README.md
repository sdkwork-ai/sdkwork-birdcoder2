# test/

Widget and integration tests for this Flutter root.

Representative screens must cover loading, success, empty, validation-error,
permission-denied, offline/unavailable, and unknown-error states
(`FLUTTER_APP_MOBILE_ARCHITECTURE_SPEC.md` §11). Run with `flutter test`.
