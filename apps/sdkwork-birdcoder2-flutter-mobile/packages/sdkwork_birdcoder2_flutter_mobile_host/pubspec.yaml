name: sdkwork_birdcoder2_flutter_mobile_host
description: Platform adapter boundary: secure storage, deep links, push, permissions, device capability probes.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  sdkwork_birdcoder2_flutter_mobile_core: { path: ../sdkwork_birdcoder2_flutter_mobile_core }

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
