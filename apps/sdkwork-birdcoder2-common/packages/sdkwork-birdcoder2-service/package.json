{
  "name": "@sdkwork/birdcoder2-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Architecture-neutral orchestration over injected SDK clients: validation mapping, cache invalidation, workflow composition.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root . --passWithNoTests"
  },
  "dependencies": {
    "@sdkwork/birdcoder2-contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
