/**
 * Public contract of `@sdkwork/birdcoder2-service`.
 *
 * Surface: `common`. Layer role: `frontend-core`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-service' as const
