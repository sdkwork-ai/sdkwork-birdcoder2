/**
 * Public contract of `@sdkwork/birdcoder2-contracts`.
 *
 * Surface: `common`. Layer role: `contracts`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-contracts' as const
