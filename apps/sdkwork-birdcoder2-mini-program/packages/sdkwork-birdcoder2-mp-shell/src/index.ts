/**
 * Public contract of `@sdkwork/birdcoder2-mp-shell`.
 *
 * Surface: `mini-program`. Layer role: `shell`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-mp-shell' as const
