{
  "name": "@sdkwork/birdcoder2-mp-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform adapter boundary (`src/weixin/` and other profiles): storage, request, login, subscribe-message, payment wrappers.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root . --passWithNoTests"
  },
  "dependencies": {
    "@sdkwork/birdcoder2-mp-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
