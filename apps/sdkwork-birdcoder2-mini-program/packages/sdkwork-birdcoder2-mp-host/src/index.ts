/**
 * Public contract of `@sdkwork/birdcoder2-mp-host`.
 *
 * Surface: `mini-program`. Layer role: `host`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-mp-host' as const
