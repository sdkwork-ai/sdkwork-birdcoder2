/**
 * Public contract of `@sdkwork/birdcoder2-mp-admin-core`.
 *
 * Surface: `mini-program`. Layer role: `runtime`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-mp-admin-core' as const
