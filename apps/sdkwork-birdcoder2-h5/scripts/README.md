# scripts/

Build, validation, generation, migration, release, and development utilities for this root.
Thin entrypoints only; reusable tooling belongs in the repository-root `tools/` or the canonical
`sdkwork-specs/tools/` implementations.
