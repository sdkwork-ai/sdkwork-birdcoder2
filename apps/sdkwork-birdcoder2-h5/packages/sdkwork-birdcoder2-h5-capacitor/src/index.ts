/**
 * Public contract of `@sdkwork/birdcoder2-h5-capacitor`.
 *
 * Surface: `h5`. Layer role: `host`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-h5-capacitor' as const
