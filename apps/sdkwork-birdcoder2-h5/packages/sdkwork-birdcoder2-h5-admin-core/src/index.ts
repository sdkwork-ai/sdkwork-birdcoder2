/**
 * Public contract of `@sdkwork/birdcoder2-h5-admin-core`.
 *
 * Surface: `h5`. Layer role: `runtime`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-h5-admin-core' as const
