/**
 * Public contract of `@sdkwork/birdcoder2-h5-console-shell`.
 *
 * Surface: `h5`. Layer role: `shell`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-h5-console-shell' as const
