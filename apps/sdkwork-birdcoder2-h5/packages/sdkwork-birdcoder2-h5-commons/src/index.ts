/**
 * Public contract of `@sdkwork/birdcoder2-h5-commons`.
 *
 * Surface: `h5`. Layer role: `frontend-commons`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-h5-commons' as const
