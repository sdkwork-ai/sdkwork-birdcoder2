# bin/android/

android helpers for `sdkwork-birdcoder2-h5`: Capacitor sync, native project open, and release packaging
hand-off. Requires the corresponding platform toolchain (Xcode / Android SDK); prerequisites
belong in the app runbook.
