# bin/

Operational helpers for `sdkwork-birdcoder2-h5`. Authority: `MODULE_BIN_SPEC.md`.

| Directory | Purpose |
| --- | --- |
| `ios/` | iOS Capacitor sync, open, archive, and signing hand-off helpers. |
| `android/` | Android Capacitor sync, open, and APK/AAB packaging helpers. |

Helpers stay thin wrappers over the standard pnpm commands declared in `package.json`.
