# bin/macos/

macos operational helpers for `sdkwork-birdcoder2-pc`: desktop launch, diagnostics bundle, and
packaging hand-off. Scripts stay thin wrappers over the standard pnpm commands declared in
`package.json`.
