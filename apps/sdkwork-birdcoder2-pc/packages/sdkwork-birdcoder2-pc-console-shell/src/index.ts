/**
 * Public contract of `@sdkwork/birdcoder2-pc-console-shell`.
 *
 * Surface: `pc`. Layer role: `shell`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-pc-console-shell' as const
