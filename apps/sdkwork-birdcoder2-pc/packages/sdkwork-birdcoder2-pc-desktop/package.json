{
  "name": "@sdkwork/birdcoder2-pc-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tauri/Electron native host: window/tray, deep links, clipboard, file dialogs, updater, local service lifecycle, tablet packaging.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root . --passWithNoTests"
  },
  "dependencies": {
    "@sdkwork/birdcoder2-pc-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
