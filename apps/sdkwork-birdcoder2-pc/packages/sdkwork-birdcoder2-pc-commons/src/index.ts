/**
 * Public contract of `@sdkwork/birdcoder2-pc-commons`.
 *
 * Surface: `pc`. Layer role: `frontend-commons`.
 * The scaffold reserves the module boundary; the first capability adds the contract.
 */
export const packageId = '@sdkwork/birdcoder2-pc-commons' as const
