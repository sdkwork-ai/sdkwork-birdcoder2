1. 在 `ui-layout` 的 sidebar owner props 中增加 `showWorkspaces: boolean`，由 `AppFrame` 按 `panels.mode === 'code'` 传值。
2. 在 `SidebarRoot` 中仅条件渲染 `sidebar.workspaces`；保留模块图标导航、sidebar 本体、New Session、footer 和中间弹性容器，避免切换模块时布局跳动。
3. 只更新两处现有的聚焦测试：验证 `AppFrame` 在 `code`/其他模式传递正确值，以及 `SidebarRoot` 隐藏 workspace 后其余 sidebar 控件仍存在。补齐受类型影响的测试 props，不新增文档或 Agent Note。
4. 运行这两个测试文件和相关的聚焦类型检查。