1. 修正 Docker 的 Landlock 打包输入
   - 修改 `Dockerfile`，不再只执行 `pnpm --dir native/landlock-run/packages/entry pack`。
   - 在 build 阶段调用现有的 `native/landlock-run/scripts/pack-release.mjs`，让它按既有设计打包两个平台包和 entry 包，并保留平台二进制的可执行权限。
   - 确保 Docker 的 npm 离线安装使用 Landlock 的完整 tarball 集合，而不是只有 entry tarball。
   - 保持 `npm install` 为纯本地 tarball 安装，不增加 npm registry 发布或 Docker registry 推送。

2. 让本地打包路径可复现并覆盖同一问题
   - 使用当前主机的 native 构建产物运行 Landlock 专用打包脚本。
   - 检查每个 tarball 的 `package/package.json`，确保 `dependencies`、`optionalDependencies` 和 `peerDependencies` 中没有 `workspace:*`。
   - 用普通 `npm install --package-lock=false` 在仓库外临时目录安装全部本地 tarball，验证 entry 包、平台包和 launcher 可执行性。
   - 对 Docker 使用的完整 dsh/vendor/Landlock tarball集合执行同等离线安装，确保容器中的 npm 阶段不会再次遇到 `EUNSUPPORTEDPROTOCOL`。

3. 增加回归保护
   - 更新 `scripts/release/verify-container.ts`，将 Dockerfile 对 Landlock 专用打包脚本和完整 Landlock tarball集合的要求写入静态验证。
   - 在已有发布验证中加入对容器输入中 workspace 协议的检查，或复用现有 `verifyPackedManifest` 逻辑，避免只在 Docker 构建后期才发现问题。
   - 如测试结构允许，增加一个针对 Docker 打包命令/输入路径的 focused test；不修改通用 workspace 依赖规则，源代码继续保留 `workspace:*`，由 pnpm pack 在发布边界转换。

4. 文档和 Agent Note
   - 更新受影响的 Landlock packaging/release 文档，明确容器离线镜像必须使用三包完整发布集合，并通过普通 npm 安装验证。
   - 为这一非机械的发布流程修复新增双语 implemented Agent Note 及 `.i18n.yaml` sidecar，记录 Docker 不能只打包 entry 包的原因和验证要求。

5. 本地验证与提交
   - 运行 Landlock focused build/test/pack/install 验证。
   - 运行 `scripts/release/verify-container` 相关测试、workflow 静态测试和必要的 TypeScript 检查。
   - 检查 git diff，确保不包含 `arm64-job.log`、`tmp-pack-test/`、`.zcode/plans` 或未跟踪的 `ui-token-plan` 包，也不提交任何凭据。
   - 提交并推送修复到 `main`；不改写 `rc.15`、`rc.16`、`rc.17`、`rc.18` 或失败的 `rc.19` 标签。

6. 新标签和 GitHub Release 验证
   - 在修复提交上创建下一个新的不可变 `birdcoder-v0.1.0-rc.20` 标签并推送，保留所有历史 release/tag。
   - 监控 workflow，确认六个平台桌面包、Compose/Kubernetes 包、Docker amd64/arm64 离线归档及 SHA-256/SHA-512 校验文件全部成功。
   - 检查 GitHub Release 恰好包含工作流要求的 31 个资产，并确认没有 npm publish 或 Docker registry push。