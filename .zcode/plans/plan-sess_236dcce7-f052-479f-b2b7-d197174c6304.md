1. 保持 Token Plan 作为固定 rail entry，不把登录态、IAM session、API 配置或 `EnvService.isConfigured()` 用作显示条件；这些状态只能影响页面内容和购买操作。
2. 在 `ui-token-plan` 增加插件 wiring 回归测试，验证 `apply()` 在匿名 IAM session、无 session、无 API token 的情况下仍注册 `mode.rail.entry`，并验证 dispose 后才移除注册。
3. 在 `ui-app-modes`/rail 相关测试中明确验证 `MODE_ORDER` 中 `token-plan` 位于最后一个普通入口，且 `mode.rail.settings` 仍由独立 bottom seat 渲染，从而保证 Token Plan 固定在 Settings 正上方。
4. 检查并修正 assembled web fixture 或端到端模式测试，使其加载 Token Plan 及 `ui-env`、`ui-iam` 硬依赖，并在未登录状态下点击入口后能看到 Token Plan 页面，而不是只验证已登录路径。
5. 运行新增 focused tests、Token Plan typecheck、client bundle/assembly checks 和 `git diff --check`；如发现显示条件被其他 composition 逻辑过滤，只修改 mode/插件装配，不改变 commerce 登录保护逻辑。