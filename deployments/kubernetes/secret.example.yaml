# Copy this file to a private location, replace the placeholder, and apply it.
# Do not commit a populated Secret. The Deployment expects this exact name/key.
apiVersion: v1
kind: Secret
metadata:
  name: dsh-credentials
type: Opaque
stringData:
  DEEPSEEK_API_KEY: replace-me
