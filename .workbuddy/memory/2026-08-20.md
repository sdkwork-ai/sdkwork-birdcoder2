# 2026-08-20 工作日志

## 自定义插件分析（fork 自 deepseek-harness）
- 分叉点 merge-base：`141eb6fef`（2026-08-19，上游 rc.8）。`upstream`=deepseek-harness，`origin`=sdkwork-ai/sdkwork-birdcoder2。
- 方法：对比 `packages/` 子包集合（fork 点 vs 当前），新增子包即自定义插件。排除 12 个 typert 测试夹具。
- **结论：共 20 个自定义插件**。
  - 桌面基础设施(3)：`boot/sdkwork-env-bootstrap`、`host/desktop-carrier`、`bundle/desktop-app`
  - 客户端 UI 模式(17，均在 `packages/client/`)：`ui-app-modes`、`ui-appstore`、`ui-assets`、`ui-course`、`ui-drive`、`ui-knowledge`、`ui-generations-image`、`ui-generations-video`、`ui-generations-assets`、`ui-iam`、`ui-token-plan`、`ui-feedback`、`ui-env`、`ui-settings-menu`、`ui-updater`、`ui-window-controls`、`ui-common-app-header`
- 详细报告见仓库根 `SDKWORK_CUSTOM_PLUGINS.md`。
- 其余 124 个 SDKWork 提交多为发布/同步噪声（siblings pin、版本重钉），非新增插件。

## 自定义插件命名规范改造（同日，已交付）
- 规范：UI 包 → `packages/client/ui-sdkwork-*` / `@deepseek-ai/dsh-client-ui-sdkwork-*` / cordis id `ui-sdkwork-*`；基础设施 → `sdkwork-desktop-carrier`、`sdkwork-desktop-app`（`@deepseek-ai/dsh-sdkwork-*`）；`sdkwork-env-bootstrap` 保持。
- 执行：git mv 19 目录 + 全仓文本替换（主仓 24 文件/371 处、.agents notes 53 文件/273 处）+ 4 个文件改名（desktop-app*.spec.ts、desktop-carrier.spec.ts、ui-iam.e2e.ts）+ `pnpm install --lockfile-only` 重生成 lockfile + 全量 install 重链 node_modules。
- 坑：pnpm 会被 WorkBuddy 的 NODE_OPTIONS safe-delete shim 拦截 → 用 `NODE_OPTIONS="--use-system-ca"`（去掉 --require genie-safe-delete）即可；git 偶发 `.git/index.lock` 陈旧锁 → 确认无 git 进程后 rm 即可。
- 回归：typecheck（tsc -b）由后台任务 WRM7nH 验证；旧名零残留；上游 `ui-theme`/`ui-sidebar`/`ui-message-feedback` 等原生 ui-* 包未动。
