{
  // Both faces of a split client package. `src/index.ts` is the Host half: it
  // keeps the durable deployment-environment section in the profile-backed
  // forms and publishes the launch-environment projection to the page.
  // `src/client` is the browser half: it provides `ctx.env` from that section.
  // The two programs merge cordis Context under the same keys (the Host half
  // needs the web server's injection event), so they meet only through the
  // shared `src/env-settings.ts` contract and never in one program.
  "files": [],
  "references": [
    {
      "path": "./tsconfig.host.json"
    },
    {
      "path": "./tsconfig.client.json"
    }
  ]
}
